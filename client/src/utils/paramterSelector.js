export function topRightGraphTypeFn(
  topRightGraphType,
  hotBlastFlow,
  hotBlastTemp,
  hotBlastPre,
  steamTph,
  co,
  co2,
  h2,
  topGasPR,
  eTaco,
  pciValue,
  raft,
  o2
) {
  if (topRightGraphType === 'HOT BLAST FLOW') {
    return hotBlastFlow;
  } else if (topRightGraphType === 'HOT BLAST TEMP') {
    return hotBlastTemp;
  } else if (topRightGraphType === 'HOT BLAST PRE') {
    return hotBlastPre;
  } else if (topRightGraphType === 'STEAM TPH') {
    return steamTph;
  } else if (topRightGraphType === 'CO') {
    return co;
  } else if (topRightGraphType === 'CO2') {
    return co2;
  } else if (topRightGraphType === 'H2') {
    return h2;
  } else if (topRightGraphType === 'TOP GAS PR') {
    return topGasPR;
  } else if (topRightGraphType === 'HOT BLAST PRE') {
    return hotBlastTemp;
  } else if (topRightGraphType === 'ETACO') {
    return eTaco;
  } else if (topRightGraphType === 'PCI') {
    return pciValue;
  } else if (topRightGraphType === 'O2 ENRICHMENT') {
    return o2;
  } else if (topRightGraphType === 'RAFT') {
    return raft;
  }
}

export function bottomLeftGraphTypeFn(
  topRightGraphType,
  hotBlastFlow,
  hotBlastTemp,
  hotBlastPre,
  steamTph,
  co,
  co2,
  h2,
  topGasPR,
  eTaco,
  pciValue,
  raft,
  o2
) {
  if (topRightGraphType === 'HOT BLAST FLOW') {
    return hotBlastFlow;
  } else if (topRightGraphType === 'HOT BLAST TEMP') {
    return hotBlastTemp;
  } else if (topRightGraphType === 'STEAM TPH') {
    return steamTph;
  } else if (topRightGraphType === 'CO') {
    return co;
  } else if (topRightGraphType === 'CO2') {
    return co2;
  } else if (topRightGraphType === 'H2') {
    return h2;
  } else if (topRightGraphType === 'TOP GAS PR') {
    return topGasPR;
  } else if (topRightGraphType === 'HOT BLAST PRE') {
    return hotBlastTemp;
  } else if (topRightGraphType === 'ETACO') {
    return eTaco;
  } else if (topRightGraphType === 'PCI') {
    return pciValue;
  } else if (topRightGraphType === 'O2 ENRICHMENT') {
    return o2;
  } else if (topRightGraphType === 'RAFT') {
    return raft;
  }
}

export function bottomRightGraphTypeFn(
  topRightGraphType,
  hotBlastFlow,
  hotBlastTemp,
  hotBlastPre,
  steamTph,
  co,
  co2,
  h2,
  topGasPR,
  eTaco,
  pciValue,
  raft,
  o2
) {
  if (topRightGraphType === 'HOT BLAST FLOW') {
    return hotBlastFlow;
  } else if (topRightGraphType === 'HOT BLAST TEMP') {
    return hotBlastTemp;
  } else if (topRightGraphType === 'STEAM TPH') {
    return steamTph;
  } else if (topRightGraphType === 'CO') {
    return co;
  } else if (topRightGraphType === 'CO2') {
    return co2;
  } else if (topRightGraphType === 'H2') {
    return h2;
  } else if (topRightGraphType === 'TOP GAS PR') {
    return topGasPR;
  } else if (topRightGraphType === 'HOT BLAST PRE') {
    return hotBlastTemp;
  } else if (topRightGraphType === 'ETACO') {
    return eTaco;
  } else if (topRightGraphType === 'PCI') {
    return pciValue;
  } else if (topRightGraphType === 'O2 ENRICHMENT') {
    return o2;
  } else if (topRightGraphType === 'RAFT') {
    return raft;
  }
}
